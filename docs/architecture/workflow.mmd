---
config:
  flowchart:
    curve: linear
---
graph TD;
	__start__([<p>__start__</p>]):::first
	_init(_init)
	route(route)
	diagnosis(diagnosis)
	planner(planner)
	tool_agent(tool_agent)
	chat_answer(chat_answer)
	fan_out_experts(fan_out_experts)
	expert_a(expert_a)
	expert_b(expert_b)
	cross_review_a(cross_review_a)
	cross_review_b(cross_review_b)
	expert_a_revise(expert_a_revise)
	expert_b_revise(expert_b_revise)
	joint_synthesis(joint_synthesis)
	judge(judge)
	revise_experts(revise_experts)
	lightweight_review(lightweight_review)
	feedback(feedback)
	finalize(finalize)
	__end__([<p>__end__</p>]):::last
	__start__ --> _init;
	_init --> route;
	cross_review_a --> expert_a_revise;
	cross_review_b --> expert_b_revise;
	diagnosis -.-> __end__;
	diagnosis -.-> planner;
	expert_a --> cross_review_a;
	expert_a_revise --> joint_synthesis;
	expert_b --> cross_review_b;
	expert_b_revise --> joint_synthesis;
	fan_out_experts --> expert_a;
	fan_out_experts --> expert_b;
	feedback --> finalize;
	joint_synthesis --> judge;
	judge -.-> feedback;
	judge -.-> lightweight_review;
	lightweight_review --> revise_experts;
	planner --> tool_agent;
	revise_experts --> joint_synthesis;
	route -.-> diagnosis;
	route -.-> tool_agent;
	tool_agent -.-> chat_answer;
	tool_agent -.-> fan_out_experts;
	chat_answer --> __end__;
	finalize --> __end__;
	classDef default fill:#f2f0ff,line-height:1.2
	classDef first fill-opacity:0
	classDef last fill:#bfb6fc
